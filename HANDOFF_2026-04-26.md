# Nuke MCP — Handoff 2026-04-26 (end of session)

## Branch state

- Branch: `feature/foundation-hardening` (5 commits ahead of `master`, NOT pushed)
- HEAD: `69ddc00` (typed handlers + read_comp single-pass + scene digest)
- Tests: 267 pass / 1 skip (live contract — `NUKE_BIN` unset) / 11 xfail (C1 placeholders)
- Total coverage: 89%, all per-file gates green

## Commit stack on this branch

| Commit | Phase | What landed |
|---|---|---|
| 008e086 | A1 | AST+regex safety scanner |
| 649da34 | A2 | retry+backoff, request_id, heartbeat, per-class timeouts, SO_KEEPALIVE |
| d8cd027 | B1+B6 | response truncation + annotation presets |
| 8143cd6 | A4 | MockNukeNode + 7-module tests + contract runner |
| 69ddc00 | A3+B7 | typed addon handlers + single-pass read_comp + scene digest |

Plus pre-branch checkpoint `c4b2e22` on master (Josh's pre-existing nuke.nodes refactor + set_node_position tool + Windows tempdir).

## Resume point — GPT-5.5 review fixes

GPT-5.5 reviewed the branch and returned **13 findings (7 critical / 6 important / minors)**. Fix-it agent was dispatched and then user-rejected before any patches landed. Re-dispatch tomorrow.

Review document is in chat history (after prompt at `GPT55_REVIEW_PROMPT.md`). Findings summary:

### Critical (must fix before merge)
1. **[SECURITY]** Safety scanner misses indirection: eval/exec, walrus, globals/vars/sys.modules, import-as alias, dangerous-returner functions, unicode homoglyphs
2. **[SECURITY]** `open()` write-mode regex broken: false-positives on `open("a")`, false-negatives on `open("foo","r+")` — replace with AST-based detection
3. **[SECURITY]** TCL pre-flight `_TCL_PYTHON_RE` misses `python {...}` brace form
4. **[BUG]** Auto-reconnect replays non-idempotent `setup_*` mutations on timeout — must skip replay when `_class not in ("read","ping")`
5. **[BUG]** `send_raw()` bypasses request_id validation + structured envelope; `render_frames()` uses `timeout=300.0` instead of `_class="render"` (900s)
6. **[RACE]** `_stop_heartbeat()` joins itself when called from heartbeat-thread failure path → `RuntimeError`
7. **[SECURITY]** `_handle_setup_write` only rejects `..`, accepts `C:\Windows\System32\...`, UNC paths, `/etc/passwd`

### Important
8. **[GAP]** `setup_*` family annotated `IDEMPOTENT` but creates new nodes every call — change to `BENIGN_NEW = {"destructiveHint": False}`
9. **[GAP]** Tests pin mock behavior, not addon behavior — add direct addon-handler tests
10. **[SMELL]** `scene_delta` test name says "without enumeration" but addon enumerates every time
11. **[GAP]** Houdini parity: `sys.modules[...]` bypass not caught
12. **[SMELL]** request_id `uuid4().hex[:8]` = 32 bits → ~34% collision over 1 minute at 1000 req/s — bump to `[:16]`
13. **[GAP]** Coverage gates can run against stale `.coverage` file

### Minors
- probe executor `max_workers` 2→4 to match Houdini
- explicit tests for `note_font*` matching `note_font` and `postage_stamp_*` NOT matching `postage_stamp`
- `response.py` "deepest children first" doc claim — implement or delete (recommend delete)
- `test_setup_keying_idempotent_re_run` rename to reflect actual duplicate-dispatch behavior
- `test_deep.py` / `test_tracking.py` xfails could pin signatures

## Pending after fixes land

Plan file: `C:\Users\Josh\.claude\plans\i-am-now-using-cuddly-cray.md`

1. **A5** crash watchdog + `crash_marker.json` + reconnect "session lost" warning (small, finishes Phase A)
2. **B2 Tasks primitive on `render_frames`** — highest-leverage modernization (first DCC MCP to ship MCP 2025-11-25 Tasks, no competitor has it)
3. **B3** schema-from-signature decorator (MayaMCP pattern)
4. **B4** skill profiles / paginated tool exposure
5. **B5** Pydantic structured outputs
6. **Phase C** Salt Spill domain depth (C1 atomic primitives `tracking.py` + `deep.py` flips the 11 xfailed tests green)
7. **Phase D** discovery + 90-second Salt Spill demo loop

## Sequence to resume tomorrow

```
cd C:\Users\Josh\Documents\nuke-mcp
git status                    # confirm clean on feature/foundation-hardening
git log --oneline c4b2e22..   # confirm 5-commit stack still intact
.venv\Scripts\activate
pytest tests/ -q              # confirm 267/1/11 baseline
# then re-dispatch fix-it agent for GPT-5.5 review (prompt below)
```

Fix-it agent prompt is preserved in chat history — search for "Apply GPT-5.5 review fixes". Re-spawn with `subagent_type: general-purpose`, `model: opus`. Prompt is self-contained and lists all 13 findings + suggested fix order (12 → 1+11 → 2 → 3 → 7 → 6 → 4 → 5 → 8 → 9 → 10 → 13 → minors).

## State to NOT lose

- `addon.py` has post-rejection edits (SO_KEEPALIVE refactored into `_enable_keepalive(sock)` helper at lines 58-85, `_dispatch` echoes `_request_id` per A2). These are expected — not Josh's work, not GPT-5.5's, just clean post-A2 shape after the previous agent's run.
- Plan file untouched.
- `GPT55_REVIEW_PROMPT.md` at repo root — defensive-framing version that bypassed cybersec flag.

## Goodnight
