# Nuke MCP v0.2.0 — Handoff (2026-04-27 evening, going to sleep)

## State at sign-off

- **Master HEAD:** `b2f935e` — pushed to `cian-bit/nuke-mcp`.
- **Tag:** `v0.2.0` annotated, **LOCAL ONLY** (not pushed).
- **Tests:** 648 passed / 1 skipped / 0 xfailed. Coverage 90%.
- **Pre-commit:** clean across every commit. No `--no-verify` used anywhere.
- **Tools shipped:** 96 across 10 profiles + 8 MCP prompts.
- **Worktrees:** all cleaned. Branches deleted.

## Roadmap status — DONE

Full roadmap from `C:\Users\Josh\.claude\plans\i-am-now-using-cuddly-cray.md` shipped:

- Phase A1-A5 (foundation hardening + GPT-5.5 review fixes + live-Nuke fixes)
- Phase B1-B7 (truncation, Tasks primitive, registry, profiles, Pydantic, annotations, speed)
- Phase C1-C10 (atomic primitives, OCIO, AOV, distortion, tracking workflow, deep workflow, CopyCat ML, salt-spill macros, audit, prompts)
- Phase D (README rewrite, DEMO screenplay, awesome-mcp + mcp.so + announcement copy, CHANGELOG, v0.2.0 tag)

## What needs doing tomorrow

### 1. GPT-5.5 ultra review (HIGH PRIORITY)

Prompt is ready at: `C:\Users\Josh\Documents\nuke-mcp\GPT55_REVIEW_2026-04-27_v0.2.0.md`

Workflow:
- Paste the prompt to GPT-5.5 with Codex / agent execution rights.
- Agent creates worktree `../nuke-mcp-gpt55-v0.2.0` on branch `review/gpt55-v0.2.0`.
- Agent reviews 12 angles, edits files, runs pytest, commits.
- Agent reports structured Markdown back to me.
- I review the commits + cherry-pick / merge into master + push.

### 2. Live Nuke smoke for C2-C10 (HIGH PRIORITY)

C1 was live-verified on 2026-04-27 against Nuke 15.1v2 (Josh's `ss_0030` comp). Three Foundry API mismatches were caught and fixed (PlanarTracker class, solveCamera button knob, DeepHoldout2). C2-C10 have NOT been live-tested yet.

Likely Foundry-API edges to test:
- C2: `nuke.root()['workingSpaceLUT']` knob name on different OCIO configs.
- C2: `nuke.root()['OCIO_config']` value() shape — string or path?
- C3: real `Read.metadata()` exr/* keys — does the canonical Karma layer list match?
- C4: `LensDistortion` node class in Nuke 15.x (or `OFXcom.foundry.nuke.LensDistortion` in some builds).
- C4: `IDistort` UV channel knob names — `uv` vs `channels0`/`channels1`?
- C4: `STMapGenerator` class fallback — does it exist in 15.1v2?
- C6: ZDefocus `math` knob — verify `depth` is a valid value, `depth_channel` knob name.
- C7: `Inference` node class for Cattery models — does it exist? Or use `CopyCat` directly?
- C8: every macro composes the above — only smoke-test once C2-C7 are confirmed.

Process for live test:
1. Pull master to `~/.nuke/nuke_mcp_addon/` (overwrite the install).
2. Restart Nuke OR hot-reload via `execute_python` (pattern from 2026-04-27 evening session).
3. Save current comp as `ss_0030_v0.2.0_smoketest.nk` to preserve the live-tested file.
4. Run smoke harness: call each new tool via `execute_python` → addon handler.
5. Fix any class-name / knob-name mismatches the same way C1 was fixed (try real first, fall back via try/except).
6. Commit fixes on a `fix/v0.2.0-live-nuke` branch, merge, push.

### 3. Push the v0.2.0 tag

After GPT-5.5 review fixes + live-Nuke fixes land, tag is updated to point at the new HEAD:

```
cd C:\Users\Josh\Documents\nuke-mcp
git tag -d v0.2.0  # delete local tag
git tag -a v0.2.0 -m "v0.2.0 - feature complete"  # re-tag at current HEAD
git push origin master --tags
```

### 4. Discovery submissions

After v0.2.0 tag is pushed:
- `docs/discovery/awesome-mcp-pr.md` — paste PR text to `awesome-mcp-servers/awesome-mcp-servers` repo.
- `docs/discovery/mcpso-listing.md` — submit to `mcp.so` (needs screenshots from live demo).
- `docs/discovery/announcement.md` — Twitter/Reddit/HN posts.

### 5. Demo video

The README + DEMO.md reference `https://youtu.be/PLACEHOLDER`. Need to:
- Record the 90-second Salt Spill demo loop following `DEMO.md` screenplay.
- Upload to YouTube.
- Replace the placeholder URL in README + mcp.so listing.

## Known minor issues (deferred)

- v0.2.0 release commit subject `release(d): v0.2.0 -- changelog and version bump for feature-complete release` is 76 chars (4 over the 72-char target). Cosmetic. Don't amend mid-stream.
- A `cancel_render` addon command name was kept rather than renamed to `cancel_task`; widened to scan both `_active_renders` and `_active_distortion_tasks` registries. Future cleanup.
- `setup_write` writes to `~/.nuke_mcp/stmaps/` as last-resort fallback for distortion C4 — confirm this is desired (alternative: hard-fail without `$SS` set).
- Wheel build doesn't ship the `.md` prompts files — `pyproject.toml` needs a `package-data` entry. C10 agent flagged this. Out of scope until first wheel publish.

## Worktrees / branches now cleared

Local checkout is fully clean:
```
$ git worktree list
C:/Users/Josh/Documents/nuke-mcp b2f935e [master]

$ git branch
* master
```

## Files of interest

- `C:\Users\Josh\Documents\nuke-mcp\GPT55_REVIEW_2026-04-27_v0.2.0.md` — review prompt for tomorrow.
- `C:\Users\Josh\.claude\plans\i-am-now-using-cuddly-cray.md` — original master plan (now fully shipped).
- `C:\Users\Josh\.claude\projects\C--Users-Josh-Documents\memory\project_mcp_creative_suite.md` — current memory.
- `C:\Users\Josh\Documents\nuke-mcp\CHANGELOG.md` — full v0.2.0 changelog by phase.

Goodnight.
